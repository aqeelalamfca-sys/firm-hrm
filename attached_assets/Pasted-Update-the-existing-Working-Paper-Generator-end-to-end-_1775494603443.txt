Update the existing Working Paper Generator end-to-end so that the uploaded Excel template becomes the single source of truth for all relevant audit variables, and all mapped values are automatically pushed into / fetched into the correct variable fields, backend entities, workflow stages, and downstream engines without manual re-entry. Retain the current UI/UX, routes, tabs, design, and overall workflow, and only strengthen the logic, backend, mapping engine, validation engine, and data persistence. The system must read the uploaded template, fully parse the engagement profile and all financial line-item rows, normalize headers, validate data types, apply alias mapping where needed, and auto-populate the relevant variables in the existing Variables / Data Extraction / Validation modules. Any field in the template that directly corresponds to an existing variable must be filled automatically with high confidence, saved in database state, reflected instantly in the frontend, and marked as template-sourced. The upload must support current fields such as entity profile, audit profile, statement type, FS section, major head, line item, sub line item, account name/code, note number, current year, prior year, debit transaction value, credit transaction value, normal balance, WP area, risk level, procedure scale, AI GL flag, GL generation priority, and remarks, so that these values flow into Trial Balance generation, General Ledger generation, working paper selection, materiality, risk, assertions, and audit procedures. Build a robust mapping layer that links template columns to variable codes, supports fallback aliases, logs unmapped fields, and never overwrites confirmed user edits unless explicitly allowed. Add strict validation rules for mandatory fields, numeric consistency, duplicate account codes, statement classification, and empty critical fields. Where template data is incomplete, AI may fill only the remaining unmapped or missing variables, but template data must always take priority over AI assumptions. Create a clear source hierarchy: Uploaded Template > Existing Confirmed Data > AI Inference > Default Logic. Ensure every successful upload triggers: (1) file parse, (2) schema validation, (3) variable mapping, (4) database save, (5) frontend refresh, (6) audit log entry, and (7) downstream recalculation of TB / GL / working paper drivers. Add status tags such as Mapped, Missing, Review, Conflict, AI-Filled, User-Edited. Provide detailed exception reporting for unmapped columns, blank mandatory values, conflicting values, and rejected rows. The process must be idempotent, so re-uploading an updated template refreshes only changed values while preserving approved overrides and maintaining version history. Update backend APIs, parser services, mapping services, variable engine, validation logic, and persistence layer accordingly. Ensure zero broken links, zero unnecessary redesign, full backward compatibility with the current code, and production-grade handling for upload-to-variable automation. The final behavior must be: upload template → extract 100% possible data → auto-fill relevant variables → flag only true exceptions → save to backend → reflect in current screens → feed TB, GL, and working paper generation automatically.