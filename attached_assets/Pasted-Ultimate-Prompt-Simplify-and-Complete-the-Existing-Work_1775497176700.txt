Ultimate Prompt — Simplify and Complete the Existing Working Paper Generator

Scan and update the existing Working Paper Generator end-to-end without unnecessary redesign, keeping the current UI style, route structure, and professional layout intact, but simplifying the workflow into only these effective sections: 1) Upload, 2) Data Extraction, 3) Trial Balance, 4) General Ledgers, 5) Working Paper Listing, 6) Working Paper Generation, 7) Export. Remove all unnecessary intermediate complexity, duplicate states, redundant review layers, and disconnected processing steps. Make the generator fully linear, practical, and production-ready. In Upload, provide a downloadable template format, template upload, and upload for other supporting files such as financial statements, sales tax returns, schedules, bank statements, annexures, and other documents. The uploaded template must be treated as the primary structured source. In Data Extraction, show all variables on one page/tab/section only; no separate validation module. Immediately auto-fetch all possible values from the uploaded template and display them in structured sections. Variables must be visibly classified into only two source types: Template Filled and AI Filled. Add a prominent AI Process / AI Fill Remaining Variables button that reads all uploaded files and fills only the remaining missing variables after template extraction. The source priority must be strictly: Template > Confirmed User Edit > AI > Default. Every variable must show label/code, value, source type, edit status, and exception flag if relevant. Keep inline review, editing, missing field handling, and conflict handling inside Data Extraction only. After Data Extraction, proceed to Trial Balance, which must open with a default structured table/format ready to populate. Add an AI Generate Trial Balance button that processes template data, uploaded files, mapped financial rows, and extracted variables to generate a balanced TB in the table. Then go to General Ledgers, which must also open with a default structured table/format and an AI Generate General Ledger button that creates transaction-wise GLs consistent with the template, TB, mapped accounts, debit/credit patterns, normal balance, and entity context. Then show Working Paper Listing, where the system lists all relevant working papers and AI recommends which working papers should be selected based on uploaded data, extracted variables, TB, GL, risk indicators, materiality, and account areas. Then in Working Paper Generation, generate working papers sequentially from Pre-Planning up to Finalization before draft report, including ML, and enforce the logic that AI completes one working paper or one working paper section fully before starting the next, with visible progress and no parallel partial generation. Then in Export, provide a clean downloadable export set consisting of: one Excel file for Trial Balance, one Excel file for General Ledger, one Excel file for relevant Working Papers, and one Word file for relevant Working Papers. Refactor the backend so that upload parsing, template mapping, AI enrichment, TB generation, GL generation, WP recommendation, WP generation, and export all work through one connected pipeline. Remove any old stage logic that does not fit this simplified flow. Ensure that the uploaded template columns and top-section data directly populate structural and financial variables, while AI fills only judgmental, descriptive, analytical, materiality, risk, sampling, procedure, and conclusion variables. Preserve audit trail, status tracking, and user overrides, but do not create extra tabs for them. Final flow must be exactly: Upload → Data Extraction → Trial Balance → General Ledgers → Working Paper Listing → Working Paper Generation → Export. The result must be simple, complete, effective, and fully aligned so that a user can upload, review, generate, and export the entire working paper file from one streamlined process.