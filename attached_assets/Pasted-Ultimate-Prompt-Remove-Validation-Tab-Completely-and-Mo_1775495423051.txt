Ultimate Prompt — Remove Validation Tab Completely and Move All Variables Under Data Extraction

Update the existing Working Paper Generator codebase to completely remove the “Validation” tab from both frontend and backend, and consolidate all variable display, review, editing, and status handling under the “Data Extraction” tab only. Do not redesign the UI unnecessarily, but fully refactor the current flow so that the tab sequence no longer includes Validation anywhere in navigation, routes, components, state, API responses, database logic, workflow stages, progress indicators, breadcrumbs, tab guards, permissions, or downstream handlers. Delete or disable all frontend elements related to Validation, including tab buttons, conditional renders, tab indexes, navigation handlers, progress-step references, active-tab logic, icons, counters, summaries, and any redirect logic pointing to Validation. Remove all backend and service-layer dependencies tied specifically to a separate Validation stage, including validation-stage status models, APIs, controllers, enums, workflow states, saved checkpoints, tab-specific fetch endpoints, and stage transition requirements. Then move all variables currently shown across Data Extraction, AI Data Sheet, and Validation into a single unified variable management interface under the Data Extraction tab, keeping the existing UI style as much as possible. Under Data Extraction, show the complete variable dataset in structured sections, including template-filled variables, AI-filled variables, missing variables, review variables, conflict variables, locked variables, user-edited variables, and system-derived variables. The Data Extraction tab must become the single control center for variable extraction, mapping, review, confirmation, editing, source tagging, exception handling, and completion status. Each variable must display its name/code, value, source, confidence/status, editability, exception flag, and any relevant note or mapping detail. Keep source tags such as Template Filled, AI Filled, Missing, Review, Conflict, User Edited, Locked, Validated if useful, but “Validated” must only be a field status, not a separate tab, module, or stage. Any logic previously handled inside the Validation tab must be merged into inline review and status handling inside Data Extraction. All exception summaries, missing field lists, conflicts, and confirmation actions must appear inside Data Extraction, preferably through filters, accordions, counters, or section blocks, without creating a separate workflow step. Update frontend state management and backend response structures so the system loads one complete variable payload for Data Extraction and does not require a second-stage Validation screen. Refactor routing and stage progression so that after Upload and AI Data Sheet, users continue directly to Trial Balance or the next downstream module, with no dependency on Validation. Ensure all current data extraction outputs, variable edits, source hierarchy, exception logs, and audit trail behavior remain intact after this change. The final behavior must be: Upload → Data Extraction (show all variables and all review/exception handling here) → AI Data Sheet → Trial Balance → General Ledger → Working Papers, with Validation fully removed from code, UI, backend logic, and workflow.