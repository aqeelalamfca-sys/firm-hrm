Update the existing Working Paper Generator end-to-end without breaking current routes, UI flow, data, or existing functionality. Redesign every working paper into a single standardized, premium, print-ready, ISA-compliant format and apply it across all phases, modules, and FS heads. Every working paper must contain, where applicable: firm name, client name, engagement code, financial year, period, WP code, WP title, phase, ISA references, status, version, lead schedule reference, FS head, sub-classification, TB codes, GL linkage, prior year reference, objective, assertion(s), risk assessment linkage, materiality linkage, population description, population size in number and amount, sample selection basis, sample selected in number and amount, audit procedures to be performed, detailed work performed, evidence obtained, test results, exceptions, variance in amount and percentage, variance explanation with management response and auditor evaluation, proposed adjustments, conclusion, remarks, cross references, review notes, prepared by, reviewed by, approved by, dates, signatures/logs, system timestamps, lock status, and AI-generated/edited tags. Make each element dynamic and rule-based so that irrelevant sections are hidden only visually but remain supported in backend schema. AI must assist in filling all elements from uploaded template, variables, TB, GL, mappings, prior year data, uploaded documents, and user inputs; however, every AI-filled field must remain editable until final lock. Strengthen the existing variable engine so that variables automatically drive assertions, risks, materiality, population, sample sizing, procedure depth, working paper selection, and wording of conclusions. Ensure complete ISA/ISQM-aligned documentation logic, especially ISA 210, 220, 230, 240, 300, 315, 320, 330, 500, 505, 510, 520, 530, 540, 550, 560, 570, 580, 600, 620, 700, 705, 706, 720 and ISQM 1/2 where applicable. Upgrade the design to a highly professional audit-file appearance with clean typography, strong hierarchy, excellent spacing, clear section dividers, print-safe margins, repeatable headers/footers, page numbering, confidential marking, sign-off blocks, and cross-reference-friendly layout. Add a true Print Preview mode and export renderer so that preview and final print/Word/PDF output always include all applicable sections, tables, narratives, sign-off areas, references, and page breaks correctly with no truncation, no hidden overflow, no missing items, and no broken styling. Add template-level and working-paper-level validation checks to enforce completeness, consistency, and compliance before finalization, including required field checks, mapping completion, TB/GL tie-out, sample coverage checks, unresolved exception alerts, unsigned review blocks, and report-blocking compliance gates only at final issuance. Keep all pages responsive on screen, but make print layout fixed, elegant, and audit-file quality. After implementing, scan the full working paper module, fix all missing elements, align all existing papers to the new master structure, backfill data where possible, preserve existing records, and repeat self-checks until the module is fully updated, visually consistent, print-complete, and 100% documentation-ready.